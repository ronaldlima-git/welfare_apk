<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.welfare">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.AdicionarEditarInstituicaoMedica"></activity>
        <activity android:name=".activities.InstituicaoMedicaActivity" />
        <activity android:name=".activities.AdicionarEditarPlanoSaude" />
        <activity android:name=".activities.AdicionarContatoActivity" />
        <activity android:name=".activities.RecuperarSenha" />
        <activity android:name=".activities.PlanoSaudeActivity" />
        <activity android:name=".activities.ContatosActivity" />
        <activity android:name=".activities.TratamentsActivity" />
        <activity android:name=".activities.ConsultasExamesActivity" />
        <activity android:name=".activities.PessoaAlterarActivity" />
        <activity android:name=".activities.MainMenuActivity" />
        <activity android:name=".activities.CadastrarActivity" />
        <activity android:name=".activities.AjudaActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>